'use strict';

window.addEventListener("load", async () => {
  // fetch("../data.json")
  //   .then(resp => resp.json())
  //   .then(data => {
  //     console.log(data.font)
  //     console.log(data.theme)
  //     document.querySelector("body").style.fontFamily = data.font
  //     document.querySelector(".tg_head_split").style.backgroundColor = data.theme

        // TODO: change dialog color to theme ------------------------------------- //
  //   })
  document.querySelector("body").style.fontFamily = "vazir"
  document.querySelector(".tg_head_split").style.backgroundColor = "black"
})
